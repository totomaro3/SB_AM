<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.KoreaIT.cwy.demo.repository.ReactionRepository">
	

	<insert id="doIncreaseGoodReaction">
		INSERT INTO reactionPoint
		SET regDate = NOW(),
		updateDate = NOW(),
		memberId = #{memberId},
		reltypeCode = 'article',
		relId = #{id},
		`point` = 1;
	</insert>
	
	<select id="isAlreadyReaction" resultType="boolean">
		Select count(*)
		FROM reactionPoint
		WHERE memberId = #{memberId} AND relId = #{id}
	</select>

</mapper>